          <tr class="<%= cycle('list_line_odd','list_line_even') %>">
            <td class='date'><%= invoice.created_at.strftime("%d-%b-%y") %></td>
            <td class='name'><%= invoice.description %></td>
            <td class='tennant'><%= name_with_initial(invoice.tennant_id)%></td>
            <td id='sum' class="<%= update_currency_class(invoice.amount,'') %>"><%=number_to_currency(invoice.amount, unit: "£")%></td>
            <td><%= link_to 'Edit', edit_invoice_path(invoice) %></td>
            <td><%= link_to 'Destroy',invoice, method: :delete, data:{confirm:'Are you sure you want to delete this invoice?'} %></td>
          </tr>